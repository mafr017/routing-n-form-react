import { NavLink } from "react-router-dom";

export default function Navbar() {

    // Func

    return (
        <div>
            PAGES NOT FOUND
            <NavLink to={'/'}>Back to Home</NavLink>
        </div>
    );
}