import { NavLink } from "react-router-dom";

export default function About() {

    // Func

    return (
        <div>
            Home
            <NavLink to={'/'}>Go to Home</NavLink>
        </div>
    );
}