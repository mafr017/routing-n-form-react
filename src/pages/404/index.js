import { NavLink } from "react-router-dom";

export default function NotFound() {

    // Func

    return (
        <div>
            PAGES NOT FOUND
            <NavLink to={'/'}>Back to Home</NavLink>
        </div>
    );
}